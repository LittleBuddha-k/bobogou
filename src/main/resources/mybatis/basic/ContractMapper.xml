<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.littlebuddha.bobogou.modules.mapper.basic.ContractMapper">
    <sql id="contractColumns">
        id AS "id",
        type AS "type",
        content AS "content",
        is_deleted AS "isDeleted",
        create_time AS "createTime",
        update_time AS "updateTime",
        operator_id AS "operatorId"
    </sql>

    <select id="get" resultType="com.littlebuddha.bobogou.modules.entity.basic.Contract">
        SELECT
        <include refid="contractColumns"/>
        FROM db_contract a
        WHERE id=#{id}
    </select>

    <select id="findList" resultType="com.littlebuddha.bobogou.modules.entity.basic.Contract">
        SELECT
        <include refid="contractColumns"/>
        FROM db_contract a
        <where>
            a.is_deleted = #{DEL_FLAG_NORMAL}
        </where>
        <choose>
            <when test="orderBy !=null and orderBy != ''">
                ORDER BY ${orderBy}
            </when>
            <otherwise>
                ORDER BY a.create_time DESC
            </otherwise>
        </choose>
    </select>

    <select id="findRecoveryList" resultType="com.littlebuddha.bobogou.modules.entity.basic.Contract">
        SELECT
        <include refid="contractColumns"/>
        FROM db_contract a
        <where>
            a.is_deleted = #{DEL_FLAG_DELETE}
        </where>
        <choose>
            <when test="orderBy !=null and orderBy != ''">
                ORDER BY ${orderBy}
            </when>
            <otherwise>
                ORDER BY a.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!--新增-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO db_contract(
            type,
            content,
            is_deleted,
            create_time,
            update_time,
            operator_id
        )VALUES (
            #{type},
            #{content},
            #{isDeleted},
            #{createTime},
            #{updateTime},
            #{operatorId}
        )
    </insert>

    <update id="update">
        UPDATE db_contract SET
            type = #{type},
            content = #{content},
            is_deleted = #{isDeleted},
            create_time = #{createTime},
            update_time = #{updateTime},
            operator_id = #{operatorId}
        WHERE id=#{id}
    </update>

    <update id="deleteByLogic">
        UPDATE db_contract SET is_deleted = #{DEL_FLAG_DELETE}
        WHERE id=#{id}
    </update>

    <update id="recovery">
        UPDATE db_contract SET is_deleted = #{DEL_FLAG_NORMAL}
        WHERE id=#{id}
    </update>

    <delete id="deleteByPhysics">
        DELETE FROM db_contract
        WHERE id=#{id}
    </delete>
</mapper>