<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.littlebuddha.bobogou.modules.mapper.other.ActMapper">
    <sql id="actColumns">
        id AS "id",
        data_id AS "data_id",
        operator_id AS "operator_id",
        operator_name AS "operatorName",
        role_id AS "role_id",
        role_name AS "role_Name",
        act_status AS "act_status",
        create_by AS "create_by",
        create_date AS "create_date",
        update_by AS "update_by",
        update_date AS "update_date"
    </sql>

    <select id="get" resultType="com.littlebuddha.bobogou.modules.entity.other.ActEntity">
        SELECT
        <include refid="actColumns"/>
        FROM act_task a
        WHERE id=#{id}
    </select>

    <select id="findList" resultType="com.littlebuddha.bobogou.modules.entity.other.ActEntity">
        SELECT
        <include refid="actColumns"/>
        FROM act_task a
        <where>

        </where>
        <choose>
            <when test="orderBy !=null and orderBy != ''">
                ORDER BY ${orderBy}
            </when>
            <otherwise>
                ORDER BY a.create_date DESC
            </otherwise>
        </choose>
    </select>

    <!--新增-->
    <insert id="insert">
        INSERT INTO act_task(
            id,
            data_id,
            operator_id,
            operator_name,
            role_id,
            role_name,
            act_status,
            create_by,
            create_date,
            update_by,
            update_date
        )VALUES (
            #{id},
            #{dataId},
            #{operatorId},
            #{operatorName},
            #{roleId},
            #{roleName},
            #{actStatus},
            #{createBy},
            #{createDate},
            #{updateBy},
            #{updateDate}
        )
    </insert>

    <update id="update">
        UPDATE act_task SET
            data_id = #{dataId},
            operator_id = #{operatorId},
            operator_name = #{operatorName},
            role_id = #{roleId},
            role_name = #{roleName},
            act_status = #{actStatus},
            create_by = #{createBy},
            create_date = #{createDate},
            update_by = #{updateBy},
            update_date = #{updateDate}
        WHERE id=#{id}
    </update>
</mapper>